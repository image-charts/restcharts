- data = data || {}

html
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible",content="IE=edge")
    meta(name="viewport",content="width=device-width")

    script(src="https://use.fontawesome.com/028467eb54.js")

    link(href="/public/css/app.css",rel="stylesheet")

    title REST Charts
  body
    div.container.small-width
      div.text-center.header
        h1 REST Charts
        div
          small Generate charts easier than ever!

      hr

      div.text-center
        h3 What is REST Charts?
        div.
          REST Charts lets you generate dynamic charts through a REST-like URL
          and query string parameters that includes information such
          as the data, chart colors, area opacity, etc.
        div.card.margin-vert-xlarge
          div.card-block
            div As easy as...
            div
              code.wrap-always(style="font-size:14px;")
                a(href=data.host + '/chart/column?data=5,1,2,9,8,3,6')= data.host + '/chart/column?data=5,1,2,9,8,3,6'

      hr

      div
        h2.text-center(id='Parameters') API
        h5 Basic Route Format
        div
          div
            code GET https://www.restcharts.com/chart/:type[?parameters]
          div or
          div
            code POST https://www.restcharts.com/chart/:type -d [parameters]
        div.margin-vert-large.text-center
          strong
            i Complete API documentation coming soon...

      hr

      h2.text-center(id='Examples') Examples
      div
        each type in data.types
          div
            h3(id=type.name)
              a(href='#' + type.name)= type.name
            div.row
              each example in type.examples
                div.col-xs-12(class=(type.name == 'Advanced Configuration') ? 'col-md-12' : 'col-md-4')
                  div.card.example.margin-vert-medium
                    - //div.card-img-top.chart-example(style="background-image:url('" + example.url + "')")
                    img.card-img-top.chart-example(class=(type.name == 'Advanced Configuration') ? 'large-img-top' : '',src=example.url)
                    div.card-block
                      a(href=example.url)
                        div.card-title
                          div= example.name
                        p.card-text
                          div.d-flex.flex-column.justify-content-center.align-items-center
                            code
                              i.fa.fa-hand-pointer-o
                              span.no-underline= ' ' + example.url
